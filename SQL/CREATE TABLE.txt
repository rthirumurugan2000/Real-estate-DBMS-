CREATE TABLE PROPERTY(
   PID INT NOT NULL,
   AGENT_ID VARCHAR (10) NOT NULL,
   NAME VARCHAR(20) NOT NULL,
   OWNER_MOBILE BIGINT NOT NULL,
   PRICE INT NOT NULL, 
   STATUS VARCHAR(8) NOT NULL,
   TYPE  VARCHAR(8)  NOT NULL,
   LIST_DATE DATE NOT NULL,
   AREA  VARCHAR (25) NOT NULL ,
   STREET VARCHAR (20)  NOT NULL,      
   PRIMARY KEY (PID) 
);

CREATE TABLE AGENT(
   ID VARCHAR(8) NOT NULL,
   NAME VARCHAR(20) NOT NULL,
   MOBILE BIGINT NOT NULL,
   PASSWORD VARCHAR(17) NOT NULL,
   NUM_PROP_SOLD  INT  NOT NULL,
   NUM_PROP_RENT  INT  NOT NULL,    
   PRIMARY KEY (ID)
);

CREATE TABLE FEATURES(
   PID INT,
   BEDROOM_NUM INT,
   BATH_NUM  INT, 
   BALCONY_NUM INT,   
   FOREIGN KEY (PID) REFERENCES PROPERTY(PID)
);

CREATE TABLE SALES(
   DEAL_NO INT,
   PID INT,
   BUYER_NAME VARCHAR(20) NOT NULL,
   BUYER_MOBILE BIGINT NOT NULL,
   DEAL_DATE DATE NOT NULL, 
   FOREIGN KEY (PID) REFERENCES PROPERTY(PID)
);


alter table property add ZIP_CODE BIGINT;
UPDATE PROPERTY SET ZIP_CODE = 625020;
UPDATE PROPERTY SET ZIP_CODE = 625021 where AREA = "SURYANAGAR";
UPDATE PROPERTY SET ZIP_CODE = 625022 where AREA = "ANNANAGAR" OR AREA = "SADASIVANAGAR";
UPDATE PROPERTY SET ZIP_CODE = 625023 where AREA = "VADIPATTI" OR AREA = "VANDIYUR";
UPDATE PROPERTY SET ZIP_CODE = 625024 where AREA = "GOMATHIPURAM" OR AREA = "GORIPALAYAM";
UPDATE PROPERTY SET ZIP_CODE = 625025 where AREA = "VILLAPURAM";

